import { PageHeading, StatusPill } from "../components/ui"; import { createClient } from "../../lib/supabase/server"; import { requireUser } from "../../lib/auth"; import { MilestonePanel, MilestoneActions } from "./MilestonePanel";
export default async function Milestones(){const user=await requireUser();const supabase=createClient();const [{data},{data:profile}]=await Promise.all([supabase.from("milestones").select("id,name,target_date,status,notes").order("target_date"),supabase.from("users").select("role").eq("id",user.id).single()]);const canManage=profile?.role==="admin";return <><PageHeading eyebrow="Planning" title="Milestones" subtitle="The key checkpoints between today and final delivery." action={canManage?<MilestonePanel/>:undefined}/><div className="card table-card">{data?.length ? <table className="table"><thead><tr><th>Milestone</th><th>Target date</th><th>Status</th><th>Notes</th>{canManage&&<th>Actions</th>}</tr></thead><tbody>{data.map(m=><tr key={m.id}><td>{m.name}</td><td>{m.target_date}</td><td><StatusPill value={m.status}/></td><td>{m.notes||""}</td>{canManage&&<td><MilestoneActions milestone={m}/></td>}</tr>)}</tbody></table> : <p className="subtitle" style={{padding:20}}>No milestones have been created yet.</p>}</div></>}
