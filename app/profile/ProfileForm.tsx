"use client";
import { useState } from "react";
import { createClient } from "../../lib/supabase/browser";
export function ProfileForm({ email, displayName, availability }: { email: string; displayName: string; availability: string }) {
  const [name,setName]=useState(displayName); const [password,setPassword]=useState(""); const [status,setStatus]=useState(availability); const [message,setMessage]=useState(""); const [error,setError]=useState("");
  async function submit(e:React.FormEvent){e.preventDefault();setMessage("");setError("");const supabase=createClient();const {data:{user}}=await supabase.auth.getUser();if(!user){setError("Your session has expired. Please sign in again.");return;}const {error:profileError}=await supabase.from("users").update({display_name:name,availability_status:status,last_seen_at:new Date().toISOString()}).eq("id",user.id);if(profileError){setError(profileError.message);return;}if(password){if(password.length<8){setError("Use at least 8 characters for a new password.");return;}const {error:passwordError}=await supabase.auth.updateUser({password});if(passwordError){setError(passwordError.message);return;}setPassword("");}setMessage("Profile saved.");}
  return <form className="card form-card" onSubmit={submit}><div className="form-grid"><div className="field full"><label>Email</label><input value={email} readOnly style={{background:"#f7f9fb"}}/></div><div className="field full"><label>Display name</label><input required value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>Availability</label><select value={status} onChange={e=>setStatus(e.target.value)}><option value="available">Available</option><option value="away">Away</option><option value="busy">Busy</option><option value="offline">Offline</option></select></div><div className="field"><label>New password</label><input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Leave blank to keep current"/></div></div>{error&&<p style={{color:"#d95561",fontSize:12}}>{error}</p>}{message&&<p style={{color:"#29926d",fontSize:12}}>{message}</p>}<button className="btn btn-primary" style={{marginTop:20}}>Save changes</button></form>;
}
